<h1>Your daily feedack analysis</h1>
        <div>
            <% for entry in @entries %>
              <h2><%= entry.title %></h2>
                <p><%= entry.created_at %></p>
                <p><%= entry.contents %></p>
            <% end %>
        </div>
        
        <div>
            <h1>My favourite websites</h1>
            <% for url, name in @websites %>
              <li><a href="<%= url %>"><%= name %></a></li>
            <% end %>
      </div>
      <img src="http://railsgirls.com/images/rg-warsaw.png" />